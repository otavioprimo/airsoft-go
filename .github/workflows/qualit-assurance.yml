name: Quallity Assurance

on:
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Running tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn test
